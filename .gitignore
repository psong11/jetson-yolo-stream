# Python
venv/
__pycache__/
*.pyc

# YOLO model weights (large binary files — don't commit)
*.pt
*.onnx
*.engine

# OS files
.DS_Store

# Test images (but keep first_observations/)
test.jpg

# Claude Code — local machine state (permissions allowlist)
.claude/settings.local.json
